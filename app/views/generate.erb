

<body>

<div class = "container">
  <div class = "row">
    <div class = "col-sm-4"> <% @photos.each do |photo| %>
        <img src="<%= photo %>" alt="random photo" />
        <form action='/generate/new' method='post'>
        <button class = "btn-default">Caption It!</button></form>
    <% end %>
  </div>

    <div class = "col-sm-4"><% @photos.each do |photo| %>
        <img src="<%= photo %>" alt="random photo" />
      </a>
    <% end %>
  </div>

    <div class = "col-sm-4"><% @photos.each do |photo| %>
        <img src="<%= photo %>" alt="random photo" />
      </a>
    <% end %>
  </div>
</div>
</div>

</body
