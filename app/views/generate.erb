

<body>

<div class = "container">
  <div class = "row">
    <div class = "col-sm-4"> <% @photos.each do |photo| %>
        <img src="<%= photo %>" alt="random photo" />
        <form class = "btn-form" action='/images' method='post'>
          <input type='hidden' name='image' value='<%= photo %>'>
          <button type="submit" class="btn1 btn1-primary btn1-lg outline">Caption It!</button>
        </form>
    <% end %>
  </div>

    <div class = "col-sm-4"><% @photos.each do |photo| %>
        <img src="<%= photo %>" alt="random photo" />
      </a>
    <% end %>
  </div>

    <div class = "col-sm-4"><% @photos.each do |photo| %>
        <img src="<%= photo %>" alt="random photo" />
      </a>
    <% end %>
  </div>
</div>
</div>

</body
