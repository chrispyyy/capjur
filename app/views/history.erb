<div class = "container">
  <div class ="image-container">
    <% @captions_url_votes.each do |caption| %>
      <img id = "user_images" src="<%=caption.image.url %>"/>
        <div class = "caption-box caption">
        <span = class="badge badge-count vote-badge">Votes: <%=caption.total_votes%></span>
        <%=caption.text %>
        </div>
        <%end %>
    </div>
</div>
